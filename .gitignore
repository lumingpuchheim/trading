# downloaded market data and derived caches (rebuild with download_data.py / screener.py / learn.py)
data/
__pycache__/
.pytest_cache/

# simulator runtime state (local account, not source)
sim/sim.db
sim/exports/
data_live/
sim/jobs.log

# bet ledger and its windows: derived caches, not source.
# rebuild with:  python minervini_bets.py --windows 252
results/minervini_bets_v5r.csv
results/minervini_bets_v5r_windows.npz

# the fit cache: rebuilt on demand, ~1 GB (fitcache.py, modelstore.py)
results/.fitcache/
